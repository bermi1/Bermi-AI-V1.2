{
  "name": "bermi-ai",
  "version": "1.2.0",
  "private": true,
  "description": "Bermi AI \u2014 Claude-style chat interface with document generation, powered by OpenRouter",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": "22.x"
  }
}