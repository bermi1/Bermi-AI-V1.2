# Server-side only. NEVER expose this to the client/browser.
OPENROUTER_API_KEY=

# Port for the Express backend (Vite dev server proxies /api to this)
PORT=3001

# Optional: path to a Chromium/Chrome binary for PDF export.
# Falls back to common locations if unset.
CHROME_PATH=
